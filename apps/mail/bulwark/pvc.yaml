apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bulwark-data
  namespace: mail
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
