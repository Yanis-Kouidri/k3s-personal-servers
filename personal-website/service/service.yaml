apiVersion: v1
kind: Service
metadata:
  name: personal-website-service
  namespace: portfolio
spec:
  selector:
    app: personal-website
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
